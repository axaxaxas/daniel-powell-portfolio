<%= form_for(@markov) do |f| %>
  <% if @markov.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@markov.errors.count, "error") %> prohibited this markov from being saved:</h2>

      <ul>
      <% @markov.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :author %><br />
    <%= f.text_field :author %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
